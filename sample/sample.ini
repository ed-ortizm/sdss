[directories]
user = /home/edgar
work = ${user}/spectra-processing

data = ${user}/spectra
meta_data = ${data}/meta_data

[files]
spectra_df = sample_galaxies.csv.gz

[redshift]
lower_bound = 0.01
upper_bound = inf

[signal_to_noise]
lower_bound = 10
upper_bound = inf


[parameters]
number_processes = 2
number_spectra = -1
