[common]
name_df = galaxy_science
ae = autoencoders-outlier-detection

[directories]
user = /home/edgar
work = ${user}/sdss

data = ${user}/spectra
meta_data = ${data}/meta_data
output = ${user}/spectra

[files]
spectra_df = ${common:name_df}.csv.gz

[redshift]
lower_bound = 0.01
upper_bound = 0.5

[signal_to_noise]
lower_bound = 4
upper_bound = inf
