[common]
name_df = 0_01_z_0_5_4_0_snr_inf

[directories]
user = /home/edgar
work = ${user}/spectra-processing

data = ${user}/spectra
raw_spectra = ${data}/raw_galaxies

process = ${data}/process
output = ${user}/spectra/${common:name_df}
meta_data = ${output}

[files]
spectra_df = ${common:name_df}.csv.gz
spectra = interpolated_spectra.npy
interpolation_config = interpolate.ini

[processing]
gaussian_filter = True
drop_spectra = 0.1
drop_waves = 0.1

[parameters]
interpolate = True
processes = 96
number_spectra = -1
